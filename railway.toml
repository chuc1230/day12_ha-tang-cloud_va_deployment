[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "sh -c 'streamlit run Lab_Assignment/group_project/chatbot/app.py --server.port $PORT --server.address 0.0.0.0'"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
